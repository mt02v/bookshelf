<%= form_with(model: book_shelf) do |form| %>
  <% if book_shelf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book_shelf.errors.count, "error") %> prohibited this book_shelf from being saved:</h2>

      <ul>
        <% book_shelf.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :book_id %>
    <%= form.text_field :book_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
